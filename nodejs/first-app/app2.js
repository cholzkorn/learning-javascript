// console.log('Hello ' + name); // global object

// other global objects:
	// setTimeout();
	// clearTimeout();
	// setInterval();
	// clearInterval();

// window.console.log
// window.setTimeout

var message = '';
console.log(global.message);